{
  "name": "logger",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "source": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf dist",
    "watch": "tsup --watch",
    "lint": "eslint \"src/**/*.ts*\"",
    "test": "vitest"
  },
  "devDependencies": {
    "config": "*",
    "tsconfig": "*",
    "typescript": "^4.5.3",
    "tsup": "7.2.0",
    "vitest": "^0.34.6"
  }
}
